-- Initialize database
-- This script runs when the PostgreSQL container is first created

-- The database and user are already created by the environment variables
-- We can add any additional setup here if needed

-- Create a simple test to verify the database is working
SELECT 'Database music_reviews created successfully' as message;